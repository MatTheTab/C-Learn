name: Neuron_tests
on: [push, pull_request]

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Build and run tests
      run: |
        g++ -o test.exe NeuronTest.cpp
        ./test.exe
